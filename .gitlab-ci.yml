image: flutterwave/flw-wildfly:latest

stages:
  - build

build_apk:
  stage: build
  script:
    - flutter --version
    - flutter pub get
    - flutter build apk --release
  artifacts:
    paths:
      - build/app/outputs/flutter-apk/app-release.apk